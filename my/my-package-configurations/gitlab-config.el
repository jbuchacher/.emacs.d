;;; gitlab-config --- Summary
;;; Commentary:
;;; Code:
(use-package gitlab
  :ensure t
  :config
  (custom-set-variables
   '(gitlab-host "https://gitlab.com")
   '(gitlab-token-id "ToSzU5jy96iqmFczsyY_"))
  )

(provide 'gitlab-config)
;;; gitlab-config.el ends here
